const API_VERSION = 'v2';
const CONTRACT_VERSION = 'phase2-traveler-api-v2-draft-1';
const DATASET_VERSION = 'urbanagent-poi-master-v1';
const DEFAULT_LIMIT = 20;
const MAX_LIMIT = 100;
const DEFAULT_RECOMMENDATION_LIMIT = 5;
const MAX_RECOMMENDATION_LIMIT = 20;
const DEFAULT_TRIP_PREVIEW_RECOMMENDATION_LIMIT = 12;
const MAX_TRIP_PREVIEW_RECOMMENDATION_LIMIT = 30;
const TRIP_PREVIEW_PACE_DEFAULT_STOPS = Object.freeze({
  relaxed: 3,
  balanced: 4,
  packed: 6,
});

const CAPABILITY_STATES = Object.freeze({
  UNAVAILABLE: 'unavailable',
  PLANNED: 'planned',
  EXPERIMENTAL: 'experimental',
  AVAILABLE: 'available',
});

const PHASE2_CITY_STATUS = 'EXPERIMENTAL';

module.exports = {
  API_VERSION,
  CAPABILITY_STATES,
  CONTRACT_VERSION,
  DATASET_VERSION,
  DEFAULT_LIMIT,
  DEFAULT_RECOMMENDATION_LIMIT,
  DEFAULT_TRIP_PREVIEW_RECOMMENDATION_LIMIT,
  MAX_LIMIT,
  MAX_RECOMMENDATION_LIMIT,
  MAX_TRIP_PREVIEW_RECOMMENDATION_LIMIT,
  PHASE2_CITY_STATUS,
  TRIP_PREVIEW_PACE_DEFAULT_STOPS,
};
